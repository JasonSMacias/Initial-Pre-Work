// In this lesson I learned how to use the == equality operator to test if two values are equal, and how it used type coercion to compare data of different types

// Setup
function testEqual(val) {
  if (val == 12) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testEqual(10);
