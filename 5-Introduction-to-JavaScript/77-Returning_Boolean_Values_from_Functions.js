// In this lesson I learned a concise way to return booleans from functions by returning a statement using a === operator

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
